/**
 * 服务注册函数
 * 向express中注册路由
 */
function register(app){

  app.listen('/login', () => {

  })

  app.listen('/register', () => {

  })

  app.listen('/validate', () => {

  })
}

module.exports = register